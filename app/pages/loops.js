// const isAdmin = true;

// if (isAdmin) {
// 	alert (`Привет Admin!`);
// } else {
// 	alert (`Привет, ты не Админ`);
// }

const isAdmin = true;
const result = (isAdmin)? `Привет Admin!` : `Привет, ты не Админ`
alert(result)